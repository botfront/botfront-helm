apiVersion: v1
kind: Service
metadata:
  labels:
    app: botfront-api
  name: {{ .Release.Name }}-api-service
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: botfront-api
  type: NodePort
