apiVersion: v1
kind: Service
metadata:
  labels:
    app: botfront-app
  name: {{ .Release.Name }}-app-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: botfront-app
  type: NodePort
